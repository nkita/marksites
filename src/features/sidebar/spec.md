# sidebar仕様

## 責務

目次とコメントを収容する右サイドバーのマークアップ、スタイル、ブラウザ動作を所有する。

## ファイル

- `index.ts`: サイドバーFeatureを構成する内部エントリポイント。

右サイドバーは選択中のパネル内容に応じた高さとし、ビューポート内の最大高を超える場合だけパネル内部をスクロールさせる。パネルコンテナは子パネルを交差軸へstretchせず先頭へ配置し、子パネルの高さ上限だけをコンテナに合わせる。目次やコメントが少ない場合、およびコメントのアーカイブを閉じた場合は、残りの高さまでパネルを引き伸ばさない。
