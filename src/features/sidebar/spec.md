# sidebar仕様

## 責務

目次とコメントを収容する右サイドバーのマークアップ、スタイル、ブラウザ動作を所有する。

## ファイル

- `index.ts`: サイドバーFeatureを構成する内部エントリポイント。

右サイドバーは選択中のパネル内容に応じた高さとし、ビューポート内の最大高を超える場合はタブ領域を固定したまま選択中のパネルだけをスクロールさせる。サイドバー本体、パネルコンテナ、選択中のパネルは`min-height: 0`と残り高さを受け取るflex指定を持ち、長い目次やコメントが親要素を押し広げないようにする。
